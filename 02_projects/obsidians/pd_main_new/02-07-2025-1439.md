```php
                $cashPlannedId = App_Db_CashPlanned::obtain()->getRowField('id', [ 'reason = ?' => $this->_getClaim()->id]);

                App_Db_CashPlanned::obtain()->update([
                    'user_id' => App_User_Wrapper::systemUserId(),
                    'date_accepting' => (new DateTime())->getTimestamp(),
                ],['id = ?' => $cashPlannedId]);

                App_Db_CashPlannedDocument::obtain()->insert([
                    'claim_id' => $this->_getClaim()->id,
                    'planned_id' => $cashPlannedId,
                    'filename' => 'Заглушка',
                    'path' => '/',
                    'type' => 'file'
                ]);
```