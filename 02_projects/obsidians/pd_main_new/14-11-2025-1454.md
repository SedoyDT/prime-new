```php

<?php  
  
use App\Avito\MessengerService;  
/**  
 * Модуль песочница. * Не заливайте изменения в репозиторий. */class Development_IndexController extends Zend_Controller_Action  
{  
    #[\DI\Attribute\Inject()]  
    protected MessengerService $messengerService;  
  
    public function init()  
    {  
        // Доступ только для администраторов  
        $auth = Zend_Auth::getInstance();  
  
        if ($auth->getIdentity()->role != 3) {  
            $this->_redirect('/');  
        }  
    }  
  
  
    /**  
     * @throws \Amd\AmdAvito\AvitoMessenger\ApiException  
     */  
    public function indexAction()  
    {  
        $this->messengerService->sendMessage(1935,'текст сообщения',1160);  
    }  
  
    public function testWarningAction(): void  
    {  
        $a = [];  
        echo $a['a'];  
  
        $b = false;  
        echo $b->user_id;  
  
        exit;  
    }  
}
```