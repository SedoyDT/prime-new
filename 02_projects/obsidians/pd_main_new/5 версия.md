```plantuml 
start

:Определить тип задачи;
if (Творческая задача?) then (Да)
    :Проверить наличие функциональной фиксации;
    if (Есть функциональная фиксация?) then (Да)
        :Преодолеть функциональную фиксацию;
        :1. Описать задачу по-другому;
        :2. Искать аналогии;
        :3. Играть с решением;
        :4. Переключать режимы мышления;
    endif
    
    :Выбрать тип творческого мышления;
    if (Нужно одно решение?) then (Да)
        :Использовать конвергентное мышление;
    else
        :Использовать дивергентное мышление;
        :Генерировать множество идей;
    endif
    
    :Применить латеральное мышление;
    :Выбрать инструменты;
    if (Нужно разрушить паттерны?) then (Да)
        :Использовать дерейлмент;
    elseif (Нужна фокусировка?) then (Да)
        :Использовать метод ассоциаций/аналогий;
    else
        :Использовать SCAMPER;
    endif
else (Нет)
    :Применить метод Декарта;
    :1. Правило очевидности;
    :2. Правило анализа;
    :3. Правило последовательности;
    :4. Правило полноты;
endif

:Выбрать стадию творческого процесса;
if (Подготовка?) then (Да)
    :Формулировать задачу;
elseif (Инкубация?) then (Да)
    :Накапливать энергию;
elseif (Решение?) then (Да)
    :Формулировать идею;
else
    :Шлифовать окончательную форму;
endif

:Применить логический метод;
if (Логика истинности?) then (Да)
    :Анализировать истинностные функции;
elseif (Логика отношений?) then (Да)
    :Анализировать отношения между объектами;
else
    :Анализировать изменяющееся знание;
endif

:Проверить результат;
if (Решение найдено?) then (Да)
    stop
else
    :Вернуться к началу;
endif

```