
#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Какие методы планирования вызова существуют?
<!-- basicblock-start oid="ObsAGjie4NTqavr1hEK8PnFE"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Какие методы планирования вызова существуют?::


Для этого существуют два метода:

    setTimeout позволяет вызвать функцию один раз через определённый интервал времени.
    setInterval позволяет вызывать функцию регулярно, повторяя вызов через определённый интервал времени.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что называется планированием вызова?
<!-- basicblock-start oid="ObskXShAxzffy04Hd4NtucNV"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что называется планированием вызова?::


Мы можем вызвать функцию не в данный момент, а позже, через заданный интервал времени. Это называется «планирование вызова».
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# f
<!-- basicblock-start oid="ObsBrXwkF4ZjV5HOajE6y76c"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Напишите функцию `sum`, которая бы работала следующим образом: 

```javascript
sum(1)(2) == 3; // 1 + 2
sum(1)(2)(3) == 6; // 1 + 2 + 3
sum(5)(-1)(2) == 6
sum(6)(-1)(-2)(-3) == 0
sum(0)(1)(2)(3)(4)(5) == 15
```
P.S. Подсказка: возможно вам стоит сделать особый метод преобразования в примитив для функции.

::

```javascript
function sum(a) {

  let currentSum = a;

  function f(b) {
    currentSum += b;
    return f;
  }

  f.toString = function() {
    return currentSum;
  };

  return f;
}

alert( sum(1)(2) ); // 3
alert( sum(5)(-1)(2) ); // 6
alert( sum(6)(-1)(-2)(-3) ); // 0
alert( sum(0)(1)(2)(3)(4)(5) ); // 15
```

Пожалуйста, обратите внимание на то, что функция `sum` выполняется лишь однажды и просто возвращает функцию `f`.

Далее, при каждом последующем вызове, `f` суммирует свой аргумент со значением `currentSum` и возвращает себя же.

**В последней строке `f` нет никакой рекурсии.**

Вот как выглядит рекурсия:

```javascript
function f(b) {
  currentSum += b;
  return f(); // <-- рекурсивный вызов
}
```

В нашем случае мы просто возвращаем функцию, не вызывая её:

```javascript
function f(b) {
  currentSum += b;
  return f; // <-- не вызывает себя. Просто возвращает
}
```

Функция `f` будет использоваться в последующем вызове и снова возвращать себя столько раз, сколько будет необходимо. Затем, при использовании в качестве числа или строки, метод `toString` возвращает `currentSum` – число. Также здесь мы можем использовать `Symbol.toPrimitive` или `valueOf` для преобразования.

<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Как это сделать? P.S. Подсказка: возможно вам стоит сделать особый метод преобразования в примитив для функции.
<!-- basicblock-start oid="Obs9Rx0rQf747Dl7aui1Fyti"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Как это сделать? P.S. Подсказка: возможно вам стоит сделать особый метод преобразования в примитив для функции.::


Чтобы реализовать метод, который превращает функцию в примитивное значение в JavaScript, можно воспользоваться специальным методом `Symbol.toPrimitive`. Этот метод позволяет настроить поведение объекта при преобразовании его в примитивное значение (например, при использовании объекта в выражениях, требующих строку, число или логическое значение).

### Что такое `Symbol.toPrimitive`?

`Symbol.toPrimitive` — это символ, используемый для определения функции, которая возвращает примитивное значение при преобразовании объекта. Когда объект используется в контексте, требующем примитивного значения, JavaScript пытается вызвать этот метод.

### Как сделать функцию с преобразованием в примитив?

Рассмотрим пример создания функции, которая преобразуется в примитивное значение (например, число):

```

// Определяем функцию
function myFunction() {
  return "This is my function";
}

// Добавляем метод Symbol.toPrimitive к функции
myFunction[Symbol.toPrimitive] = function(hint) {
  console.log(`Conversion hint: ${hint}`);
  // Проверяем, что нужно вернуть в зависимости от типа преобразования
  if (hint === "number") {
    return 42; // Например, возвращаем число
  } else if (hint === "string") {
    return "My function as a string"; // Возвращаем строку
  } else {
    return null; // По умолчанию можно вернуть null
  }
};

// Используем функцию в числовом контексте
console.log(+myFunction); // 42

// Используем функцию в строковом контексте
console.log(`${myFunction}`); // "My function as a string"

// Проверка поведения в логическом контексте
console.log(myFunction == 42); // true, так как символ преобразования возвращает 42 в числовом контексте

```

### Объяснение примера

1. **Создание функции**:
   - Определяем функцию `myFunction`.

2. **Добавление метода `Symbol.toPrimitive`**:
   - Мы добавляем метод `Symbol.toPrimitive` к функции `myFunction`, чтобы настроить её поведение при преобразовании в примитивное значение.
   - Этот метод принимает один аргумент `hint`, который подсказывает, какое примитивное значение требуется (например, `"number"`, `"string"`, или `"default"`).

3. **Использование функции в различных контекстах**:
   - Когда мы используем `+myFunction`, JavaScript вызывает `Symbol.toPrimitive` с подсказкой `"number"`, и метод возвращает `42`.
   - Когда мы используем шаблонную строку `${myFunction}`, JavaScript вызывает `Symbol.toPrimitive` с подсказкой `"string"`, и метод возвращает строку `"My function as a string"`.
   - В других контекстах JavaScript может использовать подсказку `"default"`.

Таким образом, можно определить специальный метод преобразования в примитив для функции (или любого другого объекта), чтобы настроить её поведение в различных контекстах.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Задача
<!-- basicblock-start oid="ObsLiiTLQhYVycdhEkgJlmKx"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Установка и уменьшение значения счётчика

важность: 5

Измените код `makeCounter()` так, чтобы счётчик мог уменьшать и устанавливать значение:

- `counter()` должен возвращать следующее значение (как и раньше).
- `counter.set(value)` должен устанавливать счётчику значение `value`.
- `counter.decrease()` должен уменьшать значение счётчика на 1.

Посмотрите код из песочницы с полным примером использования.

P.S. Для того, чтобы сохранить текущее значение счётчика, можно воспользоваться как замыканием, так и свойством функции. Или сделать два варианта решения: и так, и так.

::

```JS
  function makeCounter() {
    // let count = 0;
    function counter() {
      counter.set = function(val) {
        counter.count = val;
      }
      counter.decrease = function(val) {
        return counter.count--;
      }
      return counter.count++
    }
    counter.count = 0;
    return counter;
  }
  let counter = makeCounter();
  console.log( counter() ); // 0
  console.log( counter() ); // 1
  counter.set(10); // установить новое значение счётчика
  console.log( counter() ); // 10
  counter.decrease(); // уменьшить значение счётчика на 1
  console.log( counter() ); // 10 (вместо 11)
```

```javascript
function makeCounter() {
  let count = 0;

  function counter() {
    return count++;
  }

  counter.set = value => count = value;

  counter.decrease = () => count--;

  return counter;
}
```

<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что в общем можно сказать про NFE?
<!-- basicblock-start oid="ObsXipgxPxova056lwYg8Wew"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что в общем можно сказать про NFE?::


Функции – это объекты.

Их свойства:

    name – имя функции. Обычно берётся из объявления функции, но если там нет – JavaScript пытается понять его из контекста.
    length – количество аргументов в объявлении функции. Троеточие («остаточные параметры») не считается.

Если функция объявлена как Function Expression (вне основного потока кода) и имеет имя, тогда это называется Named Function Expression (Именованным Функциональным Выражением). Это имя может быть использовано для ссылки на себя же, для рекурсивных вызовов и т.п.

Также функции могут содержать дополнительные свойства. Многие известные JavaScript-библиотеки искусно используют эту возможность.

Они создают «основную» функцию и добавляют множество «вспомогательных» функций внутрь первой. Например, библиотека jQuery создаёт функцию с именем $. Библиотека lodash создаёт функцию _, а потом добавляет в неё _.clone, _.keyBy и другие свойства (чтобы узнать о ней побольше см. документацию). Они делают это, чтобы уменьшить засорение глобального пространства имён посредством того, что одна библиотека предоставляет только одну глобальную переменную, уменьшая вероятность конфликта имён.

Таким образом, функция может не только делать что-то сама по себе, но также и предоставлять полезную функциональность через свои свойства.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что можно сказать про NFE?
<!-- basicblock-start oid="ObsOjY8mTdsT47l12pn9Gkpr"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что можно сказать про NFE?::


Named Function Expression или NFE – это термин для Function Expression, у которого есть имя.
Например, давайте объявим Function Expression:

```

let sayHi = function(who) {
  alert(`Hello, ${who}`);
};

```

И присвоим ему имя:

```

let sayHi = function func(who) {
  alert(`Hello, ${who}`);
};

```

Чего мы здесь достигли? Какова цель этого дополнительного имени func?

Для начала заметим, что функция всё ещё задана как Function Expression. Добавление "func" после function не превращает объявление в Function Declaration, потому что оно все ещё является частью выражения присваивания.

Добавление такого имени ничего не ломает.

Функция все ещё доступна как sayHi():


```

let sayHi = function func(who) {
  alert(`Hello, ${who}`);
};

sayHi("John"); // Hello, John

```

Есть две важные особенности имени func, ради которого оно даётся:

    Оно позволяет функции ссылаться на себя же.
    Оно не доступно за пределами функции.

Например, ниже функция sayHi вызывает себя с "Guest", если не передан параметр who:

```

let sayHi = function func(who) {
  if (who) {
    alert(`Hello, ${who}`);
  } else {
    func("Guest"); // использует func, чтобы снова вызвать себя же
  }
};

sayHi(); // Hello, Guest

// А вот так - не cработает:
func(); // Ошибка, func не определена (недоступна вне функции)

```

Почему мы используем func? Почему просто не использовать sayHi для вложенного вызова?

Вообще, обычно мы можем так поступить:

```

let sayHi = function(who) {
  if (who) {
    alert(`Hello, ${who}`);
  } else {
    sayHi("Guest");
  }
};

```

Однако, у этого кода есть проблема, которая заключается в том, что значение sayHi может быть изменено. Функция может быть присвоена другой переменной, и тогда код начнёт выдавать ошибки:

```

let sayHi = function(who) {
  if (who) {
    alert(`Hello, ${who}`);
  } else {
    sayHi("Guest"); // Ошибка: sayHi не является функцией
  }
};

let welcome = sayHi;
sayHi = null;

welcome(); // Ошибка, вложенный вызов sayHi больше не работает!

```

Так происходит, потому что функция берёт sayHi из внешнего лексического окружения. Так как локальная переменная sayHi отсутствует, используется внешняя. И на момент вызова эта внешняя sayHi равна null.

Необязательное имя, которое можно вставить в Function Expression, как раз и призвано решать такого рода проблемы.

Давайте используем его, чтобы исправить наш код:

```

let sayHi = function func(who) {
  if (who) {
    alert(`Hello, ${who}`);
  } else {
    func("Guest"); // Теперь всё в порядке
  }
};

let welcome = sayHi;
sayHi = null;

welcome(); // Hello, Guest (вложенный вызов работает)

```



Теперь всё работает, потому что имя "func" локальное и находится внутри функции. Теперь оно взято не снаружи (и недоступно оттуда). Спецификация гарантирует, что оно всегда будет ссылаться на текущую функцию.

Внешний код все ещё содержит переменные sayHi и welcome, но теперь func – это «внутреннее имя функции», таким образом она может вызвать себя изнутри.



Это не работает с Function Declaration

Трюк с «внутренним» именем, описанный выше, работает только для Function Expression и не работает для Function Declaration. Для Function Declaration синтаксис не предусматривает возможность объявить дополнительное «внутреннее» имя.

Зачастую, когда нам нужно надёжное «внутреннее» имя, стоит переписать Function Declaration на Named Function Expression.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что можно сказать про пользовательские свойства функции ?
<!-- basicblock-start oid="ObsSC7nUPitWYdWyBPDE7VW9"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что можно сказать про пользовательские свойства функции ?::


Мы также можем добавить свои собственные свойства.
Давайте добавим свойство counter для отслеживания общего количества вызовов:

```

function sayHi() {
  alert("Hi");

  // давайте посчитаем, сколько вызовов мы сделали
  sayHi.counter++;
}
sayHi.counter = 0; // начальное значение

sayHi(); // Hi
sayHi(); // Hi

alert( `Вызвана ${sayHi.counter} раза` ); // Вызвана 2 раза

```


Свойство не есть переменная

Свойство функции, назначенное как sayHi.counter = 0, не объявляет локальную переменную counter внутри неё. Другими словами, свойство counter и переменная let counter – это две независимые вещи.

Мы можем использовать функцию как объект, хранить в ней свойства, но они никак не влияют на её выполнение. Переменные – это не свойства функции и наоборот. Это два параллельных мира.


Иногда свойства функции могут использоваться вместо замыканий. Например, мы можем переписать функцию-счётчик из главы Область видимости переменных, замыкание, используя её свойство:

```

function makeCounter() {
  // вместо
  // let count = 0

  function counter() {
    return counter.count++;
  };

  counter.count = 0;

  return counter;
}

let counter = makeCounter();
alert( counter() ); // 0
alert( counter() ); // 1

```

Свойство count теперь хранится прямо в функции, а не в её внешнем лексическом окружении.

Это хуже или лучше, чем использовать замыкание?

Основное отличие в том, что если значение count живёт во внешней переменной, то оно не доступно для внешнего кода. Изменить его могут только вложенные функции. А если оно присвоено как свойство функции, то мы можем его получить:



```

function makeCounter() {

  function counter() {
    return counter.count++;
  };

  counter.count = 0;

  return counter;
}

let counter = makeCounter();

counter.count = 10;
alert( counter() ); // 10

```

Поэтому выбор реализации зависит от наших целей.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что можно сказать про свойство length функции?
<!-- basicblock-start oid="ObsDNA4bF7H9b2kcV9AKdldT"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что можно сказать про свойство length функции?::


Ещё одно встроенное свойство «length» содержит количество параметров функции в её объявлении. Например:

```

function f1(a) {}
function f2(a, b) {}
function many(a, b, ...more) {}

alert(f1.length); // 1
alert(f2.length); // 2
alert(many.length); // 2

```

Как мы видим, троеточие, обозначающее «остаточные параметры», здесь как бы «не считается»

Свойство length иногда используется для интроспекций в функциях, которые работают с другими функциями.

Например, в коде ниже функция ask принимает в качестве параметров вопрос question и произвольное количество функций-обработчиков ответа handler.

Когда пользователь отвечает на вопрос, функция вызывает обработчики. Мы можем передать два типа обработчиков:

    Функцию без аргументов, которая будет вызываться только в случае положительного ответа.
    Функцию с аргументами, которая будет вызываться в обоих случаях и возвращать ответ.

Чтобы вызвать обработчик handler правильно, будем проверять свойство handler.length.

Идея состоит в том, чтобы иметь простой синтаксис обработчика без аргументов для положительных ответов (наиболее распространённый случай), но также и возможность передавать универсальные обработчики:

```

function ask(question, ...handlers) {
  let isYes = confirm(question);

  for(let handler of handlers) {
    if (handler.length == 0) {
      if (isYes) handler();
    } else {
      handler(isYes);
    }
  }

}

// для положительных ответов вызываются оба типа обработчиков
// для отрицательных - только второго типа
ask("Вопрос?", () => alert('Вы ответили да'), result => alert(result));

```

Это частный случай так называемого Ad-hoc-полиморфизма – обработка аргументов в зависимости от их типа или, как в нашем случае – от значения length. Эта идея имеет применение в библиотеках JavaScript.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что можно сказать про свойство name функции?
<!-- basicblock-start oid="ObsnibxCtfqSdoPSg9LO7iQs"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что можно сказать про свойство name функции?::


Объект функции содержит несколько полезных свойств.

Например, имя функции нам доступно как свойство «name»😂️️

```

function sayHi() {
  alert("Hi");
}

alert(sayHi.name); // sayHi

```

Что довольно забавно, логика назначения name весьма умная. Она присваивает корректное имя даже в случае, когда функция создаётся без имени и тут же присваивается, вот так:

```

let sayHi = function() {
  alert("Hi");
};

alert(sayHi.name); // sayHi (есть имя!)

```

Это работает даже в случае присваивания значения по умолчанию:

```JS
function f(sayHi = function() {}) {
  alert(sayHi.name); // sayHi (работает!)
}

f();
```

В спецификации это называется «контекстное имя»: если функция не имеет name, то JavaScript пытается определить его из контекста.

Также имена имеют и методы объекта:

```javascript
let user = {

  sayHi() {
    // ...
  },

  sayBye: function() {
    // ...
  }

}

alert(user.sayHi.name); // sayHi
alert(user.sayBye.name); // sayBye
```

В этом нет никакой магии. Бывает, что корректное имя определить невозможно. В таких случаях свойство name имеет пустое значение. Например:


```javascript
// функция объявлена внутри массива
let arr = [function() {}];

alert( arr[0].name ); // <пустая строка>
// здесь отсутствует возможность определить имя, поэтому его нет
```

Впрочем, на практике такое бывает редко, обычно функции имеют `name`.



<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# В JavaScript функции – это объекты.
<!-- basicblock-start oid="ObsKUpH2sqUsHBqVySLdYdxC"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
В JavaScript функции – это объекты.::


Можно представить функцию как «объект, который может делать какое-то действие». Функции можно не только вызывать, но и использовать их как обычные объекты: добавлять/удалять свойства, передавать их по ссылке и т.д.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Вопрос интересный, так как сравнение глобального объекта в JavaScript с инъекцией зависимостей (dependency injection) в AngularJS позволяет лучше понять обе концепции. Давайте разберёмся, в чём их различия и схожести.
<!-- basicblock-start oid="ObsES77nYhul7TXb4z9TesE4"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Вопрос интересный, так как сравнение глобального объекта в JavaScript с инъекцией зависимостей (dependency injection) в AngularJS позволяет лучше понять обе концепции. Давайте разберёмся, в чём их различия и схожести.::


### Глобальный объект в JavaScript

Глобальный объект в JavaScript представляет собой объект, доступный из любой точки программы. В зависимости от среды исполнения он имеет разные имена:
- **`window`** — в браузере.
- **`global`** — в Node.js.
- **`globalThis`** — универсальное имя, добавленное в ECMAScript, чтобы ссылаться на глобальный объект в любой среде исполнения.

#### Основные характеристики глобального объекта:
1. **Доступность отовсюду:** Любая переменная или функция, объявленная глобально или добавленная в глобальный объект, доступна из любой точки кода.
2. **Неизолированность:** Глобальные переменные и функции могут быть переопределены или изменены любым кодом, что может привести к конфликтам и непредсказуемому поведению.
3. **Использование для полифилов:** Глобальный объект можно использовать для определения, поддерживаются ли современные возможности языка в данном окружении и при необходимости добавлять полифилы.

### Инъекция зависимостей в AngularJS

**Инъекция зависимостей** (DI) в AngularJS — это метод, используемый для предоставления зависимостей (сервисов, фабрик и т.д.) компонентам AngularJS (контроллерам, директивам, фильтрам и т.п.).

#### Основные характеристики DI в AngularJS:
1. **Изоляция зависимостей:** Каждый компонент AngularJS объявляет свои зависимости в виде аргументов функции или конструктора. AngularJS автоматически передаёт необходимые зависимости при создании экземпляра компонента.
2. **Инкапсуляция и модульность:** Зависимости управляются AngularJS, что позволяет избежать использования глобальных объектов и уменьшает вероятность конфликтов.
3. **Удобство тестирования:** Компоненты легко тестировать, так как можно подменять зависимости в тестах.
4. **Позволяет определить зависимости заранее:** AngularJS делает зависимости явными, что улучшает читаемость и упрощает поддержку кода.

### Сопоставление глобального объекта и инъекции зависимостей

- **Глобальный объект** предоставляет механизмы, чтобы сделать переменные и функции доступными повсеместно, но это приводит к проблемам с поддержкой и тестированием, поскольку такой код менее модульный и инкапсулированный. Глобальные переменные могут изменяться в любом месте программы, что делает код более хрупким и трудным для отслеживания.

- **Инъекция зависимостей** в AngularJS, напротив, предоставляет контролируемый и изолированный способ передачи зависимостей, что способствует лучшей структурированности кода, его модульности и возможности легкого тестирования.

### В чем ключевые различия?

1. **Изоляция и контроль**: DI предоставляет изолированные зависимости, тогда как глобальные объекты доступны повсеместно и могут быть легко изменены любым кодом, что ведёт к риску непредсказуемого поведения.

2. **Тестируемость**: С DI можно легко подменять зависимости, что делает тестирование более прямолинейным и независимым от конкретной реализации. В случае с глобальными объектами, тестирование становится более сложным, так как глобальные состояния могут влиять на результаты.

3. **Явное объявление зависимостей**: DI делает зависимости явными, улучшая читаемость кода и его поддержку, тогда как глобальные объекты часто делают код менее прозрачным.

### Заключение

Хотя глобальный объект и инъекция зависимостей решают похожие задачи по доступу к функциям и данным из разных частей программы, **DI обеспечивает более структурированный и безопасный подход**. Использование глобального объекта следует избегать или минимизировать в современных проектах, особенно тех, которые предполагают долгосрочную поддержку и развитие.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что можно сказать про глобальный объект?
<!-- basicblock-start oid="Obsa7dYMcj0uX2q7Yb3DpGbV"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что можно сказать про глобальный объект?::




    Глобальный объект хранит переменные, которые должны быть доступны в любом месте программы.

    Это включает в себя как встроенные объекты, например, Array, так и характерные для окружения свойства, например, window.innerHeight – высота окна браузера.

    Глобальный объект имеет универсальное имя – globalThis.

    …Но чаще на него ссылаются по-старому, используя имя, характерное для данного окружения, такое как window (браузер) и global (Node.js).

    Следует хранить значения в глобальном объекте, только если они действительно глобальны для нашего проекта. И стараться свести их количество к минимуму.

    В браузерах, если только мы не используем модули, глобальные функции и переменные, объявленные с помощью var, становятся свойствами глобального объекта.

    Для того, чтобы код был проще и в будущем его легче было поддерживать, следует обращаться к свойствам глобального объекта напрямую, как window.x.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# ьо
<!-- basicblock-start oid="Obs9maSbTfdZjPeO7PuqyBBW"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
был такой код:
```javascript
function makeArmy() {
  let shooters = [];

  let i = 0;
  while (i < 10) {
    let shooter = function() { // функция shooter
      alert( i ); // должна выводить порядковый номер
    };
    shooters.push(shooter); // и добавлять стрелка в массив
    i++;
  }

  // ...а в конце вернуть массив из всех стрелков
  return shooters;
}

let army = makeArmy();

// все стрелки выводят 10 вместо их порядковых номеров (0, 1, 2, 3...)
army[0](); // 10 от стрелка с порядковым номером 0
army[1](); // 10 от стрелка с порядковым номером 1
army[2](); // 10 ...и т.д.
```
я получил задание Почините код, чтобы он работал как задумано.
```JS
function makeArmy() {
  let shooters = [];
  
  let i = 0;
  while (i < 10) {
    let j = i;
    let shooter = function() { // функция shooter
      console.log(j)
    
    };
    shooters.push(shooter); // и добавлять стрелка в массив
    i++;
  }

  // ...а в конце вернуть массив из всех стрелков
  return shooters;
}

let army = makeArmy();

// все стрелки выводят 10 вместо их порядковых номеров (0, 1, 2, 3...)
army[0](); // 10 от стрелка с порядковым номером 0
army[1](); // 10 от стрелка с порядковым номером 1
army[2](); // 10 ...и т.д.
```
правильно ли я понимаю, что решение заключается в том что во втором коде j находится в одном лексическом окружении с функцией и функция ее запоминает в том виде в каком она была когда функция была объявлена если я не прав или не до конца объяснил поправь или добавь

::

Да, вы правильно понимаете, что ваше решение работает, потому что переменная `j` находится в замыкании функции `shooter`. Давайте подробно разберёмся, почему именно так и в чём разница между первым и вторым кодом.

### Проблема в исходном коде

В вашем исходном коде:

```javascript
function makeArmy() {
  let shooters = [];
  let i = 0;
  while (i < 10) {
    let shooter = function() { 
      alert(i); // должна выводить порядковый номер
    };
    shooters.push(shooter); // и добавлять стрелка в массив
    i++;
  }

  return shooters;
}

let army = makeArmy();

army[0](); // 10
army[1](); // 10
army[2](); // 10
```

**Проблема** заключается в том, что переменная `i` объявлена вне функции `shooter`, и все функции `shooter`, добавленные в массив `shooters`, **ссылаются на одну и ту же переменную `i`**. Когда цикл завершается, значение `i` становится равным 10, и все функции `shooter` используют это значение при вызове, поэтому они все выводят `10`.

### Почему работает ваше решение

Ваше решение:

```javascript
function makeArmy() {
  let shooters = [];
  
  let i = 0;
  while (i < 10) {
    let j = i; // создаём новую переменную j
    let shooter = function() {
      console.log(j); // используем переменную j
    };
    shooters.push(shooter);
    i++;
  }

  return shooters;
}

let army = makeArmy();

army[0](); // 0
army[1](); // 1
army[2](); // 2
```

**Объяснение:**  
В вашем исправленном коде вы создаёте новую переменную `j` внутри каждого цикла `while`. Эта переменная `j` создается и сохраняется **в своём собственном лексическом окружении** для каждой функции `shooter`. 

Когда функция `shooter` создаётся, она "запоминает" (замыкает) текущее значение `j`. Поскольку для каждого цикла создаётся новая переменная `j`, каждая функция `shooter` имеет свою собственную версию этой переменной. Таким образом, они выводят правильные значения (`0`, `1`, `2` и так далее), а не `10`.

### Лексическое окружение и замыкания

Лексическое окружение — это механизм, который JavaScript использует для хранения переменных и их значений в области видимости. 

**Замыкание** — это функция, которая «запоминает» своё лексическое окружение. Это значит, что функция всегда имеет доступ к тем переменным, которые находились в области её видимости на момент создания, даже если эта функция вызывается вне своей исходной области видимости.

В вашем решении `j` создается как новая переменная в каждой итерации цикла, поэтому для каждой функции `shooter` создается своё замыкание с собственной копией `j`.

### Итог

Ваше объяснение в целом верное, но можно добавить, что каждый раз, когда вы создаёте новую переменную `j` внутри цикла, вы создаёте **новое лексическое окружение** для этой переменной. Каждая функция `shooter` сохраняет ссылку на это окружение, тем самым "замыкая" текущее значение переменной `j`. Таким образом, функция будет выводить значение, которое имела переменная `j` на момент создания функции.


<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что такое замыкание ?
<!-- basicblock-start oid="ObsIVFvokDT3Hqv8mWDG3mUJ"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что такое замыкание ?::


Замыкание — это функция, которая запоминает свое лексическое окружение, то есть контекст, в котором она была создана. В данном случае inBetween(a, b) и inArray(arr) должны вернуть функции, которые будут использовать параметры a, b, и arr при вызове filter. 

в задаче про создание arr.filter(inBetween()) arr.filter(inArray())
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что такое функция высшего порядка?
<!-- basicblock-start oid="Obs6pEq5M3xbS2cfrT1vrfpS"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что такое функция высшего порядка?::


Это функция, которая может принимать другую функцию в качестве аргумента или возвращать функцию как результат. В данном случае, arr.filter является функцией высшего порядка, так как принимает функцию f в качестве аргумента для фильтрации массива.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# В процессе вызова функции у нас есть два лексических окружения: 
<!-- basicblock-start oid="Obsj6G7kY7Wvp5Mxg1AIC8Yr"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
В процессе вызова функции у нас есть два лексических окружения: ::


В процессе вызова функции у нас есть два лексических окружения: внутреннее (для вызываемой функции) и внешнее (глобальное)😂️️


    Внутреннее лексическое окружение соответствует текущему выполнению say.

    В нём находится одна переменная name, аргумент функции. Мы вызываем say("John"), так что значение переменной name равно "John".

    Внешнее лексическое окружение – это глобальное лексическое окружение.

    В нём находятся переменная phrase и сама функция.

```

let phrase = "hello";

function say(name) {
console.log
```
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Когда запускается функция, в начале ее вызова автоматически создается новое лексическое окружение 
<!-- basicblock-start oid="ObsszENXTrRPE5pbyJrmNJIo"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Когда запускается функция, в начале ее вызова автоматически создается новое лексическое окружение ::


для хранения локальных переменных и параметров вызова.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Работа с переменными – это на самом деле
<!-- basicblock-start oid="Obs6QqWdmtANg7aU8qpM5ZL7"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Работа с переменными – это на самом деле::


Работа с переменными – это на самом деле работа со свойствами этого объекта.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Переменная – это свойство специального
<!-- basicblock-start oid="ObsPoVmwihav5To6JsmxWAPN"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Переменная – это свойство специального::


Переменная – это свойство специального внутреннего объекта, связанного с текущим выполняющимся блоком/функцией/скриптом.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Из каких двух частей состоит объект лексического окружения?
<!-- basicblock-start oid="Obsp43TiD2ikwmJXljUHJ3c0"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
В JavaScript у каждой выполняемой функции, блока кода `{...}` и скрипта есть связанный с ними внутренний (скрытый) объект, называемый _лексическим окружением_ `LexicalEnvironment`. Из каких двух частей состоит объект лексического окружения?::


Environment Record – объект, в котором как свойства хранятся все локальные переменные (а также некоторая другая информация, такая как значение this).

Ссылка на внешнее лексическое окружение – то есть то, которое соответствует коду снаружи (снаружи от текущих фигурных скобок).

"Переменная" – это просто свойство специального внутреннего объекта: Environment Record. «Получить или изменить переменную», означает, «получить или изменить свойство этого объекта».
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что такое замыкание ?
<!-- basicblock-start oid="ObsSjbe36YB347icSlyQ3YDA"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что такое замыкание ?::



Замыкания

В программировании есть общий термин: «замыкание», – который должен знать каждый разработчик.

Замыкание – это функция, которая запоминает свои внешние переменные и может получить к ним доступ. В некоторых языках это невозможно, или функция должна быть написана специальным образом, чтобы получилось замыкание. Но, как было описано выше, в JavaScript, все функции изначально являются замыканиями (есть только одно исключение, про которое будет рассказано в Синтаксис "new Function").

То есть они автоматически запоминают, где были созданы, с помощью скрытого свойства [[Environment]], и все они могут получить доступ к внешним переменным.

Когда на собеседовании фронтенд-разработчику задают вопрос: «что такое замыкание?», – правильным ответом будет определение замыкания и объяснения того факта, что все функции в JavaScript являются замыканиями, и, может быть, несколько слов о технических деталях: свойстве [[Environment]] и о том, как работает лексическое окружение.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Как выглядит полное объяснение ?
<!-- basicblock-start oid="ObsXYDn3B14761FQsd7RGu8m"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Как выглядит полное объяснение ?::


В JavaScript у каждой выполняемой функции, блока кода {...} и скрипта есть связанный с ними внутренний (скрытый) объект, называемый лексическим окружением LexicalEnvironment.

Объект лексического окружения состоит из двух частей:

    Environment Record – объект, в котором как свойства хранятся все локальные переменные (а также некоторая другая информация, такая как значение this).

    Ссылка на внешнее лексическое окружение – то есть то, которое соответствует коду снаружи (снаружи от текущих фигурных скобок).

"Переменная" – это просто свойство специального внутреннего объекта: Environment Record. «Получить или изменить переменную», означает, «получить или изменить свойство этого объекта».

Например, в этом простом коде только одно лексическое окружение:

```

let phrase = "Hello";
// phrase: "Hello";
// Лексическое окружение - phase: "Hello";
// outer - null
```
Это, так называемое, глобальное лексическое окружение, связанное со всем скриптом.

На картинке выше прямоугольник означает Environment Record (хранилище переменных), а стрелка означает ссылку на внешнее окружение. У глобального лексического окружения нет внешнего окружения, так что она указывает на null.

По мере выполнения кода лексическое окружение меняется.
Вот более длинный код:
```
 
let phrase;       // Лексическое окружение - phase: undefined;
phrase = "Hello"; // Лексическое окружение - phase: "Hello";
phrase = "bye";   // Лексическое окружение - phase: "bye";

// outer - null
```
Комментарии справа демонстрируют, как глобальное лексическое окружение изменяется в процессе выполнения кода

При запуске скрипта лексическое окружение предварительно заполняется всеми объявленными переменными.

Изначально они находятся в состоянии «Uninitialized». Это особое внутреннее состояние, которое означает, что движок знает о переменной, но на нее нельзя ссылаться, пока она не будет объявлена с помощью `let`. Это почти то же самое, как если бы переменная не существовала.

1. Появляется определение переменной `let phrase`. У неё ещё нет присвоенного значения, поэтому присваивается `undefined`. С этого момента мы можем использовать переменную.
 2. Переменной `phrase` присваивается значение.
 2. Переменная `phrase` меняет значение.

Пока что всё выглядит просто, правда?

- Переменная – это свойство специального внутреннего объекта, связанного с текущим выполняющимся блоком/функцией/скриптом.
- Работа с переменными – это на самом деле работа со свойствами этого объекта.

Лексическое окружение – объект спецификации

«Лексическое окружение» – это объект спецификации: он существует только «теоретически» в спецификации языка для описания того, как все работает. Мы не можем получить этот объект в нашем коде и манипулировать им напрямую.

JavaScript-движки также могут оптимизировать его, отбрасывать неиспользуемые переменные для экономии памяти и выполнять другие внутренние действия, но при этом видимое поведение остается таким, как описано.

### [Шаг 2. Function Declaration](https://learn.javascript.ru/closure#shag-2-function-declaration)

Функция – это тоже значение, как и переменная.
**Разница заключается в том, что Function Declaration мгновенно инициализируется полностью.**
Когда создается лексическое окружение, Function Declaration сразу же становится функцией, готовой к использованию (в отличие от `let`, который до момента объявления не может быть использован).
Именно поэтому мы можем вызвать функцию, объявленную как Function Declaration, до самого её объявления.

Вот, к примеру, начальное состояние глобального лексического окружения при добавлении функции:

```JS
// Начало выполнения: phrase = uninitialized, say: function

let phrase = "hello";

function say(name) {
console.log(`${phrase} ${name}`)
}
```
В процессе вызова функции у нас есть два лексических окружения: внутреннее (для вызываемой функции) и внешнее (глобальное):

- Внутреннее лексическое окружение соответствует текущему выполнению `say`.
    
    В нём находится одна переменная `name`, аргумент функции. Мы вызываем `say("John")`, так что значение переменной `name` равно `"John"`.
    
- Внешнее лексическое окружение – это глобальное лексическое окружение.
    
    В нём находятся переменная `phrase` и сама функция.


У внутреннего лексического окружения есть ссылка на внешнее `outer`.

**Когда код хочет получить доступ к переменной – сначала происходит поиск во внутреннем лексическом окружении, затем во внешнем, затем в следующем и так далее, до глобального.**

Если переменная не была найдена, это будет ошибкой в строгом режиме (`use strict`). Без строгого режима, для обратной совместимости, присваивание несуществующей переменной создаёт новую глобальную переменную с таким же именем.

Давайте посмотрим, как происходит поиск в нашем примере:

- Для переменной `name`, `alert` внутри `say` сразу же находит ее во внутреннем лексическом окружении.
- Когда `alert` хочет получить доступ к `phrase`, он не находит её локально, поэтому вынужден обратиться к внешнему лексическому окружению и находит `phrase` там.
  
Давайте вернёмся к примеру с `makeCounter`:

```javascript
function makeCounter() {
  let count = 0;

  return function() {
    return count++;
  };
}

let counter = makeCounter();
```

В начале каждого вызова `makeCounter()` создается новый объект лексического окружения, в котором хранятся переменные для конкретного запуска `makeCounter`.

Таким образом, мы имеем два вложенных лексических окружения, как в примере выше:

Отличие заключается в том, что во время выполнения `makeCounter()` создается крошечная вложенная функция, состоящая всего из одной строки: `return count++`. Мы ее еще не запускаем, а только создаем.

Vсе функции помнят лексическое окружение, в котором они были созданы. Технически здесь нет никакой магии: все функции имеют скрытое свойство `[[Environment]]`, которое хранит ссылку на лексическое окружение, в котором была создана функция:

Таким образом, `counter.[[Environment]]` имеет ссылку на `{count: 0}` лексического окружения. Так функция запоминает, где она была создана, независимо от того, где она вызывается. Ссылка на `[[Environment]]` устанавливается один раз и навсегда при создании функции.

Впоследствии, при вызове `counter()`, для этого вызова создается новое лексическое окружение, а его внешняя ссылка на лексическое окружение берется из `counter.[[Environment]]`:

Теперь, когда код внутри `counter()` ищет переменную `count`, он сначала ищет ее в собственном лексическом окружении (пустом, так как там нет локальных переменных), а затем в лексическом окружении внешнего вызова `makeCounter()`, где находит `count` и изменяет ее.

**Переменная обновляется в том лексическом окружении, в котором она существует.**
Вот состояние после выполнения:

Если мы вызовем `counter()` несколько раз, то в одном и том же месте переменная `count` будет увеличена до `2`, `3` и т.д.

Замыкания

В программировании есть общий термин: «замыкание», – который должен знать каждый разработчик.

[Замыкание](https://ru.wikipedia.org/wiki/%D0%97%D0%B0%D0%BC%D1%8B%D0%BA%D0%B0%D0%BD%D0%B8%D0%B5_(%D0%BF%D1%80%D0%BE%D0%B3%D1%80%D0%B0%D0%BC%D0%BC%D0%B8%D1%80%D0%BE%D0%B2%D0%B0%D0%BD%D0%B8%D0%B5)) – это функция, которая запоминает свои внешние переменные и может получить к ним доступ. В некоторых языках это невозможно, или функция должна быть написана специальным образом, чтобы получилось замыкание. Но, как было описано выше, в JavaScript, все функции изначально являются замыканиями (есть только одно исключение, про которое будет рассказано в [Синтаксис "new Function"](https://learn.javascript.ru/new-function)).

То есть они автоматически запоминают, где были созданы, с помощью скрытого свойства `[[Environment]]`, и все они могут получить доступ к внешним переменным.

Когда на собеседовании фронтенд-разработчику задают вопрос: «что такое замыкание?», – правильным ответом будет определение замыкания и объяснения того факта, что все функции в JavaScript являются замыканиями, и, может быть, несколько слов о технических деталях: свойстве `[[Environment]]` и о том, как работает лексическое окружение.


<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Вложенная функция может быть возвращена: либо
<!-- basicblock-start oid="Obsbodfika4ZYMO9stpKnQ69"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Вложенная функция может быть возвращена: либо::


либо в качестве свойства нового объекта
либо сама по себе
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Когда функция называется вложенной?
<!-- basicblock-start oid="Obs5hv2JuJrWAFHtrmKPJAK6"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Когда функция называется вложенной?::


когда она создается внутри другой функции
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что сохраняют замыкания?
<!-- basicblock-start oid="ObsEj6JbaEgeqBbdkiB2pIjB"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что сохраняют замыкания?::


ссылки на значения
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Мы знаем, что функция может получить доступ к переменным из внешнего окружения, эта возможность используется очень часто. Но что произойдёт, когда внешние переменные изменятся? Функция получит последнее значение или то, которое существовало на момент создания функции?
<!-- basicblock-start oid="ObskyD4eIeta9arqTQys23L7"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Мы знаем, что функция может получить доступ к переменным из внешнего окружения, эта возможность используется очень часто. Но что произойдёт, когда внешние переменные изменятся? Функция получит последнее значение или то, которое существовало на момент создания функции?::


В JavaScript, функции имеют доступ к переменным из своего внешнего окружения благодаря механизму, известному как **замыкание**. Когда функция создается, она "запоминает" свое лексическое окружение, то есть доступные в момент ее создания переменные. 

### Как работает доступ к внешним переменным?

1. **Функция использует текущее значение переменной:**
   - Если переменная из внешнего окружения изменяется после создания функции, но до её вызова, функция увидит это измененное значение при выполнении. Функция не "копирует" значения переменных из внешнего окружения в момент её создания; вместо этого, она имеет ссылку на эти переменные и использует их текущее значение на момент вызова.

2. **Замыкания сохраняют ссылки, а не значения:**
   - Важный момент заключается в том, что замыкания сохраняют **ссылки** на переменные, а не их значения. Это означает, что если вы измените значение переменной, на которую ссылается замыкание, функция при вызове увидит это новое значение.

### Пример

```

function makeCounter() {
  let count = 0; // переменная в лексическом окружении функции makeCounter

  return function() { // замыкание
    return count++;
  };
}

let counter = makeCounter(); // создаем замыкание
console.log(counter()); // 0
console.log(counter()); // 1

// создаем другое замыкание
let counter2 = makeCounter();
console.log(counter2()); // 0

```

В этом примере функция `makeCounter` создает переменную `count` и возвращает функцию, которая увеличивает и возвращает `count`. Обратите внимание, что каждая переменная `count` сохраняется в своём собственном замыкании, и если `count` изменится в одном замыкании, это не повлияет на `count` в другом.

### Влияние изменений переменных

Вот пример, показывающий, что происходит, когда изменяется внешняя переменная:

```

let name = "Alice";

function greet() {
  console.log("Hello, " + name);
}

greet(); // "Hello, Alice"

name = "Bob";
greet(); // "Hello, Bob"

```

В данном примере:
- Функция `greet` "запоминает" переменную `name` из своего внешнего окружения.
- Когда вызывается `greet`, она использует **текущее значение** переменной `name`.
- После изменения `name` на "Bob", вызов `greet` будет использовать обновленное значение.

### Вывод

**Функция всегда использует последнее значение внешней переменной на момент её вызова.** Поэтому, если переменная изменяется после создания функции, но до её вызова, функция будет использовать новое значение переменной.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что в общем можно сказать про оператор расширения?
<!-- basicblock-start oid="Obs2P43TiAKyoJNOGIF53ZBD"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что в общем можно сказать про оператор расширения?::


Когда мы видим "..." в коде, это могут быть как остаточные параметры, так и оператор расширения.

Как отличить их друг от друга:

    Если ... располагается в конце списка параметров функции, то это «остаточные параметры». Он собирает остальные неуказанные аргументы и делает из них массив.
    Если ... встретился в вызове функции или где-либо ещё, то это «оператор расширения». Он извлекает элементы из массива.

Полезно запомнить:

    Остаточные параметры используются, чтобы создавать новые функции с неопределённым числом аргументов.
    С помощью оператора расширения можно вставить массив в функцию, которая по умолчанию работает с обычным списком аргументов.

Вместе эти конструкции помогают легко преобразовывать наборы значений в массивы и обратно.

К аргументам функции можно обращаться и по-старому — через псевдомассив arguments.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что является главным недостатком списка?
<!-- basicblock-start oid="ObsCwSlbeOn8gnFMANfaTOkY"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что является главным недостатком списка?::


Главным списка недостатком является то, что мы не можем легко получить доступ к элементу по его индексу. В простом массиве: arr[n] является прямой ссылкой. Но в списке мы должны начать с первого элемента и перейти в next N раз, чтобы получить N-й элемент.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что можно сказать про связанный список?
<!-- basicblock-start oid="ObsaSOaARfOngzWFAYmBZy5J"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что можно сказать про связанный список?::


Представьте себе, что мы хотим хранить упорядоченный список объектов.
Естественным выбором будет массив:

```

let arr = [obj1, obj2, obj3];

```

…Но у массивов есть недостатки. Операции «удалить элемент» и «вставить элемент» являются дорогостоящими. Например, операция arr.unshift(obj) должна переиндексировать все элементы, чтобы освободить место для нового obj, и, если массив большой, на это потребуется время. То же самое с arr.shift().

Единственные структурные изменения, не требующие массовой переиндексации – это изменения, которые выполняются с конца массива: arr.push/pop. Таким образом, массив может быть довольно медленным для больших очередей, когда нам приходится работать с его началом.

Или же, если нам действительно нужны быстрые вставка/удаление, мы можем выбрать другую структуру данных, называемую связанный список.

Элемент связанного списка определяется рекурсивно как объект с:

    value,
    next – свойство, ссылающееся на следующий элемент связанного списка или null, если это последний элемент.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что такое рекурсивная структура?
<!-- basicblock-start oid="Obs6BrLjcyrWyXi1uG6uNPYx"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что такое рекурсивная структура?::


Рекурсивная (рекурсивно определяемая) структура данных – это структура, которая повторяет саму себя в своих частях.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что называют глубиной рекурсии?
<!-- basicblock-start oid="ObsFzIa32FIffiGVxDJKIrT5"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что называют глубиной рекурсии?::


Общее количество вложенных вызовов (включая первый) называют глубиной рекурсии. В нашем случае она будет равна n.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Когда используют рекурсию?
<!-- basicblock-start oid="ObsD9ovLE8Hp55DTvgQM145o"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Когда используют рекурсию?::


Итак, рекурсию используют, когда вычисление функции можно свести к её более простому вызову, а его – к ещё более простому и так далее, пока значение не станет очевидно.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Как математически можно записать потенциально рекурсивную функцию pow(x,n)?
<!-- basicblock-start oid="ObsPUb0aegElPP8OO9LMBqgk"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Как математически можно записать потенциально рекурсивную функцию pow(x,n)?::


Мы можем представить pow(x, n) в виде: x * pow(x, n - 1). Что в математике записывается как: x^n = x * x^(n-1). Эта ветвь – шаг рекурсии: мы сводим задачу к более простому действию (умножение на x) и более простой аналогичной задаче (pow с меньшим n). Последующие шаги упрощают задачу всё больше и больше, пока n не достигает 1.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Любая рекурсия может быть переделана в цикл. Как правило, вариант с циклом будет эффективнее. НО !!!
<!-- basicblock-start oid="Obst6CStZCl5HHqpew4I15dq"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Любая рекурсия может быть переделана в цикл. Как правило, вариант с циклом будет эффективнее. НО !!!::


Но переделка рекурсии в цикл может быть нетривиальной, особенно когда в функции в зависимости от условий используются различные рекурсивные подвызовы, результаты которых объединяются, или когда ветвление более сложное. Оптимизация может быть ненужной и совершенно нестоящей усилий.

Часто код с использованием рекурсии более короткий, лёгкий для понимания и поддержки. Оптимизация требуется не везде, как правило, нам важен хороший код, поэтому она и используется.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Когда функция производит вложенный вызов, происходит следующее:
<!-- basicblock-start oid="Obs8WszdY4BxHs7OHk94IQs8"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Когда функция производит вложенный вызов, происходит следующее:::


Когда функция производит вложенный вызов, происходит следующее:

    Выполнение текущей функции приостанавливается.
    Контекст выполнения, связанный с ней, запоминается в специальной структуре данных – стеке контекстов выполнения.
    Выполняются вложенные вызовы, для каждого из которых создаётся свой контекст выполнения.
    После их завершения старый контекст достаётся из стека, и выполнение внешней функции возобновляется с того места, где она была остановлена.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Что такое execution context?
<!-- basicblock-start oid="Obs60JfWU2ksaUbj0sA9iydU"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Что такое execution context?::


Контекст выполнения – специальная внутренняя структура данных, которая содержит информацию о вызове функции. Она включает в себя конкретное место в коде, на котором находится интерпретатор, локальные переменные функции, значение this (мы не используем его в данном примере) и прочую служебную информацию.
<!-- basicblock-end -->




#learnJsTasks_part_1_6_advanced_work_with_functions
#part_1_6_advanced_work_with_functions

#telegram 

# Где хранится информация о процессе выполнения запущенной функции?
<!-- basicblock-start oid="Obsl9DAWUauMVCSRET7aQdZc"  deck='learnJsTasks_part_1_6_advanced_work_with_functions' -->
Где хранится информация о процессе выполнения запущенной функции?::


Информация о процессе выполнения запущенной функции хранится в её контексте выполнения (execution context).
<!-- basicblock-end -->



