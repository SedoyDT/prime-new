
# Начало карточки

<!-- basicblock-start oid="ObslU7Fi7HNTasjUfb46tlpy"  deck='0_Pd_php_options_and_context_parameters_16' -->
Опции и параметры контекста ¶::


PHP предлагает ряд опций и параметров контекста, которые разрешается использовать со всеми файловыми системами и обёртками потоков. Контекст создаётся функцией stream_context_create(). Опции устанавливаются функцией stream_context_set_option(), а параметры — функцией stream_context_set_params().
<!-- basicblock-end -->

# Конец карточки

# Содержимое карточки
**Forwarded from [Анатолий Фролов](https://t.me/rempant)**

Опции и параметры контекста ¶

PHP предлагает ряд опций и параметров контекста, которые разрешается использовать со всеми файловыми системами и обёртками потоков. Контекст создаётся функцией stream_context_create(). Опции устанавливаются функцией stream_context_set_option(), а параметры — функцией stream_context_set_params().



