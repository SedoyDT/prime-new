<!-- basicblock-start oid="ObsfsCCoxPjAbA6E1nzTEy0H" deck='0_Pd_' -->

8.1 Вопрос: "Что такое `BIT` и как указать количество битов для этого типа данных?" ::  
8.2 Ответ: "`BIT` представляет собой тип данных для хранения битовых значений. Количество битов, которые может хранить `BIT`, указывается в квадратных скобках. Допустимое значение `M` варьируется от 1 до 64."  
Пример кода:
```sql
-- Создание таблицы с BIT
CREATE TABLE example_bit (
    flags BIT(8)  -- Хранит 8 бит
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="Obs8rHqZ3sA8prSDFCZslcF2" deck='' -->

9.1 Вопрос: "Какие данные могут храниться в типах `BOOL` и `BOOLEAN` и как они связаны с `TINYINT`?" ::  
9.2 Ответ: "`BOOL` и `BOOLEAN` являются синонимами для `TINYINT(1)`, где значение 0 считается ложным, а любые ненулевые значения считаются истинными."  
Пример кода:
```sql
-- Создание таблицы с BOOL
CREATE TABLE example_bool (
    is_active BOOL  -- Хранит значения TRUE или FALSE
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObsfN3WE97ChGryqQo8rglwA" deck='' -->

10.1 Вопрос: "Как можно использовать `ZEROFILL` для числовых типов данных и какое влияние он оказывает на вывод значений?" ::  
10.2 Ответ: "`ZEROFILL` добавляет ведущие нули к числовым значениям до достижения заданной ширины отображения. Это делает вывод значений выравненным по правому краю с ведущими нулями."  
Пример кода:
```sql
-- Создание таблицы с ZEROFILL
CREATE TABLE example_zerofill (
    id INT(5) ZEROFILL,  -- Значения будут выравнены с ведущими нулями
    score TINYINT(3) UNSIGNED ZEROFILL
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObsQd2wUWoSBLK88CSXLFb0n" deck='' -->

11.1 Вопрос: "Какие отличия между `FLOAT(p)` и `DOUBLE`?" ::  
11.2 Ответ: "`FLOAT(p)` представляет собой число с плавающей запятой, где `p` задает точность в битах и используется для выбора между `FLOAT` и `DOUBLE`. Если `p` от 0 до 24, используется `FLOAT`, если от 25 до 53, используется `DOUBLE`."  
Пример кода:
```sql
-- Создание таблицы с FLOAT(p) и DOUBLE
CREATE TABLE example_float_double (
    float_p FLOAT(24),  -- Используется FLOAT, так как p < 25
    double_col DOUBLE   -- Всегда используется DOUBLE
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObseQRSlHKIDzeFpCDp4NYA9" deck='' -->

12.1 Вопрос: "Какой диапазон значений можно хранить в `SMALLINT UNSIGNED` и `SMALLINT SIGNED`?" ::  
12.2 Ответ: "`SMALLINT UNSIGNED` может хранить значения от 0 до 65,535, в то время как `SMALLINT SIGNED` может хранить значения от -32,768 до 32,767."  
Пример кода:
```sql
-- Создание таблицы с SMALLINT SIGNED и UNSIGNED
CREATE TABLE example_smallint (
    signed_col SMALLINT SIGNED,
    unsigned_col SMALLINT UNSIGNED
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObsnippL41sbsqhxvzwovFwR" deck='' -->

13.1 Вопрос: "Что означает использование SQL режима `NO_UNSIGNED_SUBTRACTION`?" ::  
13.2 Ответ: "Когда SQL режим `NO_UNSIGNED_SUBTRACTION` активен, результат вычитания между целочисленными значениями с типом `UNSIGNED` будет также знаковым. По умолчанию результат вычитания между `UNSIGNED` значениями остается `UNSIGNED`."  
Пример кода:
```sql
-- Включение SQL режима NO_UNSIGNED_SUBTRACTION
SET sql_mode = 'NO_UNSIGNED_SUBTRACTION';

-- Создание таблицы и выполнение вычитания
CREATE TABLE example_no_unsigned (
    number1 INT UNSIGNED,
    number2 INT UNSIGNED
);

-- Пример вычитания
SELECT number1 - number2 FROM example_no_unsigned;
```

<!-- basicblock-end -->

<!-- basicblock-start oid="Obs7zbMJEz9Ib8wwpRYWPsYH" deck='' -->

14.1 Вопрос: "Как `DECIMAL` тип отличается от `FLOAT` и `DOUBLE` в контексте хранения и точности?" ::  
14.2 Ответ: "`DECIMAL` обеспечивает фиксированную точность и подходит для хранения денежных значений и других чисел, требующих точности, в то время как `FLOAT` и `DOUBLE` имеют плавающую точность, что может привести к округлению и потере точности."  
Пример кода:
```sql
-- Создание таблицы с DECIMAL, FLOAT и DOUBLE
CREATE TABLE example_precision (
    decimal_col DECIMAL(10,2),  -- Хранит точные значения с двумя десятичными знаками
    float_col FLOAT,            -- Хранит значения с плавающей точностью
    double_col DOUBLE           -- Хранит значения двойной точности
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObsAz6BWdSl0TXHB1eMWtsWk" deck='' -->

15.1 Вопрос: "Что происходит при использовании `AUTO_INCREMENT` с `SERIAL` в MySQL?" ::  
15.2 Ответ: "`SERIAL` автоматически добавляет атрибут `AUTO_INCREMENT`, который позволяет автоматически увеличивать значение поля для создания уникальных идентификаторов. Поле также автоматически получает атрибут `NOT NULL` и `UNIQUE`."  
Пример кода:
```sql
-- Создание таблицы с AUTO_INCREMENT и SERIAL
CREATE TABLE example_auto_increment (
    id SERIAL PRIMARY KEY  -- Поле будет автоматически увеличиваться
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObsJL6j5legjWnjFVguCsQ31" deck='' -->

1.1 Вопрос: "Что означает `UNSIGNED` для целочисленных типов данных в MySQL?" ::  
1.2 Ответ: "`UNSIGNED` указывает, что тип данных может хранить только неотрицательные значения, что увеличивает диапазон положительных значений по сравнению с `SIGNED`."  
Пример кода:
```sql
-- Создание таблицы с столбцом UNSIGNED
CREATE TABLE example_unsigned (
    id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
    quantity SMALLINT UNSIGNED
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObsyjgJbd0kRRamg8745vdee"  deck='' -->

2.1 Вопрос: "Чем отличается `INT` от `INTEGER` в MySQL?" ::  
2.2 Ответ: "`INT` и `INTEGER` — это синонимы и имеют одинаковое значение. Они обозначают один и тот же тип данных для хранения целых чисел."  
Пример кода:
```sql
-- Обе команды создадут идентичную таблицу
CREATE TABLE example_int (
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY
);

CREATE TABLE example_integer (
    id INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObsNDeSAbg85CgeFWdhDQgBe" deck='' -->

3.1 Вопрос: "Какова разница между `FLOAT` и `DOUBLE` типами данных?" ::  
3.2 Ответ: "`FLOAT` представляет собой число с плавающей запятой одинарной точности, в то время как `DOUBLE` представляет число с плавающей запятой двойной точности, имеющее большую точность и диапазон."  
Пример кода:
```sql
-- Создание таблицы с FLOAT и DOUBLE
CREATE TABLE example_float_double (
    float_col FLOAT,
    double_col DOUBLE
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObsghGMLQRSdMdJOUpeVLk1B"  deck='' -->

4.1 Вопрос: "Что делает атрибут `ZEROFILL` и как он взаимодействует с `UNSIGNED`?" ::  
4.2 Ответ: "`ZEROFILL` добавляет ведущие нули к числовому значению, чтобы оно соответствовало указанной ширине отображения. При этом MySQL автоматически добавляет атрибут `UNSIGNED` к столбцу, если он не был указан."  
Пример кода:
```sql
-- Создание таблицы с ZEROFILL
CREATE TABLE example_zerofill (
    id INT(5) ZEROFILL NOT NULL,
    quantity TINYINT(3) UNSIGNED ZEROFILL
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="Obs2TQc6JnIUdhYZVXqMdEOi" deck='' -->

5.1 Вопрос: "Как отличается `DECIMAL` от `FLOAT` и `DOUBLE`?" ::  
5.2 Ответ: "`DECIMAL` представляет собой точное фиксированное число с плавающей запятой, которое сохраняет точность до заданного количества десятичных знаков. В отличие от `FLOAT` и `DOUBLE`, которые имеют плавающую точность и могут терять точность при вычислениях."  
Пример кода:
```sql
-- Создание таблицы с DECIMAL, FLOAT и DOUBLE
CREATE TABLE example_decimal_float_double (
    decimal_col DECIMAL(10,2),
    float_col FLOAT,
    double_col DOUBLE
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="Obsku5SB49CWsMRcFRwpJRWL"  deck='' -->

6.1 Вопрос: "Что означает использование `SERIAL` в MySQL и как это связано с типом `BIGINT UNSIGNED`?" ::  
6.2 Ответ: "`SERIAL` является псевдонимом для `BIGINT UNSIGNED NOT NULL AUTO_INCREMENT UNIQUE`, который автоматически увеличивается при добавлении новых записей и используется для создания уникальных идентификаторов."  
Пример кода:
```sql
-- Создание таблицы с SERIAL
CREATE TABLE example_serial (
    id SERIAL PRIMARY KEY
);
```

<!-- basicblock-end -->

<!-- basicblock-start oid="ObscA7Cbcfp9cmTY1WsLzKPw" deck='' -->

7.1 Вопрос: "Как работает атрибут `SIGNED` и почему он не оказывает эффекта на числовые типы данных, которые по умолчанию signed?" ::  
7.2 Ответ: "Атрибут `SIGNED` указывает, что числовое поле может хранить как положительные, так и отрицательные значения. Однако, если тип данных по умолчанию уже signed, указание `SIGNED` не изменит его поведение."  
Пример кода:
```sql
-- Создание таблицы с SIGNED и UNSIGNED типами
CREATE TABLE example_signed_unsigned (
    signed_col INT SIGNED,
    unsigned_col INT UNSIGNED
);
```

<!-- basicblock-end -->