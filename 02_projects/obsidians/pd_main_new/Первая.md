```plantuml
interface Observable #lightblue {
  +attach(Observable $observable)
  +detach(Observable $observable)
  +notify()
}

interface Observer #lightgreen {
 +update(Observable $observable)
}

class LoginObserver {
 +update(Observable $observable)
 +doUpdate(login: Login)
 +__construct(login: Login)
}

class Login {
  +attach()
  +detach()
  +notify()
  +getStatus()
}

Login --|> Observable : Реализует
Observable o-- Observer
LoginObserver --|> Observer

Login "1" --* LoginObserver

class SecurityMonitor {
   +doUpdate(login: Login)
}

class GeneralLogger {
   +doUpdate(login: Login)
}

class PartnershipTool {
  +doUpdate(login: Login)
}
SecurityMonitor --|> LoginObserver
GeneralLogger --|> LoginObserver
PartnershipTool --|> LoginObserver
```

