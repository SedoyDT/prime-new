---
kanban-plugin: basic
tags:
- tag
- 19
---

# 📅 Неделя 19

```dataviewjs
const file = dv.current().file;

const created = file.ctime;

const year = created.year;
const month = String(created.month).padStart(2, "0");
const quarter = Math.ceil(created.month / 3);

const base = `${year}/${year}-Q${quarter}/${year}-${month}`;

const folders = new Map();

for (let f of app.vault.getAllLoadedFiles()) {
  if (f.path.startsWith(base + "/")) {
    const parts = f.path.split("/");
    if (parts.length > 4) {
      const folderPath = parts.slice(0, -1).join("/");
      const folderName = parts[parts.length - 2];
      folders.set(folderPath, folderName);
    }
  }
}

dv.table(
  ["File"],
  [...folders.entries()].map(([folderPath, name]) => {
    const filePath = `${folderPath}/${name}`;
    return [dv.fileLink(filePath)];
  })
);
```

---

## 1. 🎯 Цели недели
- [ ] AI / агенты / система #AI
- [ ] Anki регулярность #Anki
- [ ] JavaScript практика #Код
- [ ] Recovery тела + лицо #Recovery
- [ ] 1 улучшение личной системы #OS

---

## 2. 💻 Работа и развитие
- [[Проект A]]
- [[Проект B]]
- JS / код / мини-задачи #Код
- Obsidian / автоматизация / агенты #OS

---

## 3. 🧠 Память и система
```dataview
table from #19
sort file.ctime desc
```
- Anki (10–30 мин ежедневно) #Anki
- AI-концепции / карточки #Anki
- системные заметки / OS #OS

---

## 4. 🧴 Тело и восстановление
- Theragun / спина / триггеры #Тело
- компрессия ног / вибрация #Тело
- уход за лицом (ретинол / кислоты / RF) #Лицо
- 1 день полного отдыха #Тело

---

%% kanban:settings %%
```
{"kanban-plugin":"basic"}
```
%%